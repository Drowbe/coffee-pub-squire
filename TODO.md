# TODO

The list lives in **[documents/TODO.md](documents/TODO.md)**.

This file used to be a second, independently maintained list, which is how it
ended up describing work that had already shipped — the module split, the
`trayPosition` removal — alongside quest and pin items that are no longer
Squire's at all, having moved to Librarian and Blacksmith. Everything still live
was folded into `documents/TODO.md`; this stub stays so the next person to open
it is redirected rather than editing a list nobody reads.
