<div class="blacksmith-window-template-root codex-window" id="{{appId}}">
    <header class="blacksmith-window-template-header">
        <div class="blacksmith-window-template-header-content">
            <div class="blacksmith-window-template-header-icon">
                <i class="fa-solid fa-book-open-cover"></i>
            </div>
            <div class="blacksmith-window-template-header-title-block">
                <div class="blacksmith-window-template-header-title codex-window-header-title">{{headerTitle}}</div>
                {{#if subtitle}}
                    <div class="blacksmith-window-template-header-subtitle">{{subtitle}}</div>
                {{/if}}
            </div>
        </div>
    </header>

    <div class="blacksmith-window-template-body">
        <form class="codex-window-form" method="post">
            <input type="hidden" name="img" value="{{entry.img}}">
            <input type="hidden" name="pageUuid" value="{{entry.pageUuid}}">

            <section class="blacksmith-window-section codex-drag-section codex-drag-zone">
                <div class="codex-drop-callout">
                    <i class="fa-solid fa-cloud-arrow-up"></i>
                    <div class="codex-drop-copy">
                        <div class="codex-drop-title">{{#if isEditing}}Update From Drop{{else}}Drag & Drop Here{{/if}}</div>
                        <div class="codex-drop-subtitle">Drop actors, items, journal entries, or journal pages to auto-populate fields.</div>
                    </div>
                </div>
            </section>

            <section class="blacksmith-window-section codex-image-section">
                <div class="blacksmith-window-section-header">
                    <i class="fa-solid fa-image"></i>
                    <span>Image</span>
                </div>
                <div class="blacksmith-window-section-body">
                    <div class="codex-image-preview-wrapper">
                        <div class="codex-image-placeholder">No image selected. Drop a document with artwork to populate this section.</div>
                        <img class="codex-image-preview" src="" alt="Preview">
                        <button type="button" class="codex-remove-image blacksmith-window-btn-critical" aria-label="Remove image">
                            <i class="fa-solid fa-times"></i>
                        </button>
                    </div>
                    <div class="codex-image-actions">
                        <button type="button" class="blacksmith-window-btn-secondary codex-browse-image">
                            <i class="fa-solid fa-folder-open"></i>
                            <span>Browse Image</span>
                        </button>
                    </div>
                </div>
            </section>

            <section class="blacksmith-window-section">
                <div class="blacksmith-window-section-header">
                    <i class="fa-solid fa-circle-info"></i>
                    <span>Basic Information</span>
                </div>
                <div class="blacksmith-window-section-body">
                    <div class="codex-form-grid codex-form-grid-two">
                        <div class="codex-form-field codex-form-field-full">
                            <label for="name">Name *</label>
                            <input type="text" id="name" name="name" value="{{entry.name}}" required>
                        </div>

                        <div class="codex-form-row codex-form-field-full">
                            <div class="codex-form-field codex-form-row-select">
                                <label for="category">Category *</label>
                                <select id="category" name="category" required>
                                    <option value="">Select Category</option>
                                    <option value="new">+ New Category</option>
                                    {{#each existingCategories}}
                                    <option value="{{this}}" {{#if (eq this ../entry.category)}}selected{{/if}}>{{this}}</option>
                                    {{/each}}
                                </select>
                            </div>
                            <div class="codex-form-field new-category-input-field">
                                <label for="new-category">New Category *</label>
                                <input type="text" id="new-category" name="new-category" placeholder="Enter new category">
                            </div>
                            <div class="codex-form-field codex-form-field-icon new-category-icon-field">
                                <label for="categoryIcon">Icon *</label>
                                <input type="text" id="categoryIcon" name="categoryIcon" value="{{entry.categoryIcon}}" placeholder="fa-solid fa-map">
                            </div>
                        </div>

                        <div class="codex-form-field codex-form-field-full">
                            <label>Location</label>
                            <div class="codex-location-path">
                                {{#each locationLevels}}
                                {{#unless @first}}<i class="fa-solid fa-angle-right codex-location-sep"></i>{{/unless}}
                                <div class="codex-location-segment">
                                    <span class="codex-location-seglabel">{{this.label}}</span>
                                    <input type="text" name="location-{{@index}}" value="{{this.value}}"
                                        list="{{../appId}}-loc-{{@index}}" placeholder="{{this.placeholder}}" autocomplete="off">
                                    <datalist id="{{../appId}}-loc-{{@index}}">
                                        {{#each this.suggestions}}<option value="{{this}}"></option>{{/each}}
                                    </datalist>
                                </div>
                                {{/each}}
                            </div>
                            <small class="codex-form-hint">Broadest to most specific — leave trailing levels blank. Pick an existing value or type a new one.</small>
                        </div>

                        <div class="codex-form-field codex-form-field-full">
                            <label>Links <span class="codex-form-label-optional">(optional)</span></label>
                            <div class="codex-links-dropzone">
                                <div class="codex-links-list"></div>
                                <div class="codex-links-hint">
                                    <i class="fa-solid fa-link"></i>
                                    Drag actors, items, journals, or pages here to link them.
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </section>

            <section class="blacksmith-window-section">
                <div class="blacksmith-window-section-header">
                    <i class="fa-solid fa-scroll"></i>
                    <span>Content</span>
                </div>
                <div class="blacksmith-window-section-body">
                    <div class="codex-form-field">
                        <label for="description">Summary</label>
                        <textarea id="description" name="description" rows="4" placeholder="A concise summary of what this is, where it's located, and why it matters. Deeper lore belongs in the journal page's Expanded Details section.">{{#if entry.description}}{{entry.description}}{{/if}}</textarea>
                    </div>

                    <div class="codex-form-field">
                        <label for="plotHook">Plot Hook</label>
                        <textarea id="plotHook" name="plotHook" rows="3" placeholder="How does this relate to the story? What might the party need from it?">{{#if entry.plotHook}}{{entry.plotHook}}{{/if}}</textarea>
                    </div>
                </div>
            </section>

            <section class="blacksmith-window-section">
                <div class="blacksmith-window-section-header">
                    <i class="fa-solid fa-tags"></i>
                    <span>Tags</span>
                </div>
                <div class="blacksmith-window-section-body">
                    <div class="codex-form-field">
                        <label for="tags">Tags (comma-separated)</label>
                        <input type="text" id="tags" name="tags" value="{{#if entry.tags}}{{#if (isArray entry.tags)}}{{entry.tags.join ', '}}{{else}}{{entry.tags}}{{/if}}{{/if}}" placeholder="Enter tags separated by commas">
                        <small class="codex-form-hint">Click suggested tags to add or remove them.</small>
                    </div>
                    {{#if suggestedTags.length}}
                        <div class="codex-tag-suggestions-wrap">
                            <div class="codex-tag-group-label">Suggested</div>
                            <div class="blacksmith-tags" data-chip-type="tag">
                                {{#each suggestedTags}}
                                    <span class="blacksmith-tag codex-tag-chip {{#if (includes ../entry.tags this)}}active{{/if}}" data-tag-value="{{this}}">{{this}}</span>
                                {{/each}}
                            </div>
                        </div>
                    {{/if}}
                </div>
            </section>

            <section class="blacksmith-window-section">
                <div class="blacksmith-window-section-header">
                    <i class="fa-solid fa-book-open"></i>
                    <span>Expanded Details</span>
                </div>
                <div class="blacksmith-window-section-body">
                    <div class="codex-form-field">
                        <div class="codex-expanded-editor-host"></div>
                        <small class="codex-form-hint">Free-form rich lore shown on the journal page below the summary block, reachable from the tray via "Read more".</small>
                    </div>
                </div>
            </section>
        </form>
    </div>

    <section class="blacksmith-window-template-buttons">
        <div class="blacksmith-window-template-action-bar">
            <div class="blacksmith-window-template-action-left"></div>
            <div class="blacksmith-window-template-action-right">
                <button type="button" class="blacksmith-window-btn-secondary" data-action="cancel">Cancel</button>
                <button type="button" class="blacksmith-window-btn-primary" data-action="save">{{#if isEditing}}Update Entry{{else}}Save Entry{{/if}}</button>
            </div>
        </div>
    </section>
</div>
